.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
streamlit.log

# Regenerated by `python run_pipeline.py` — too large / derived to track.
data/raw/*.csv
data/processed/*.csv
data/warehouse/*.db

# Everything under reports/ IS tracked: it is the evidence of a pipeline run
# (findings.md, figures, OLAP query results, per-task metrics JSON).

# The Power BI CSV extract is derived from the warehouse (≈50 MB) — regenerate
# it with `python -m src.export_bi`.  The PBIP project itself IS tracked.
powerbi/data/
